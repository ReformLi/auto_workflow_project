# 项目总结报告

## 项目概述

基于 PyQt5 和 NodeGraphQt 的 Windows 窗口自动化工作流可视化编辑器（自动工作流设计器 v1.0.0）。用户通过拖拽节点、连线编排自动化流程，执行引擎在子线程中依次调度节点，底层通过 uiautomation、pywin32、OpenCV 完成实际的窗口控制、输入模拟与图像识别操作。

## 架构设计

分层架构，单向依赖：`ui → core → nodes → automation`，模块间通过事件总线（PyQt5 信号）解耦通信。

```
ui/         界面层：主窗口、节点库、画布、日志、对话框、样式
core/       逻辑中枢：图管理、执行引擎、工作流模型、上下文、事件总线
nodes/      节点库：自动发现注册，每个节点实现 execute(inputs)
automation/ 自动化引擎：窗口/鼠标/键盘/元素定位/图像识别
utils/      工具：safe_eval 安全求值、工作流验证
app/        入口与全局配置
```

## 已完成功能

### 1. 核心架构
- ✅ `CoreManager` 门面：组装图管理器与执行器，动态发现并注册节点
- ✅ 节点自动发现：扫描 `nodes/` 包，自动收集 `WorkflowNode` 子类注册到图中
- ✅ 事件总线：`execution_started/finished`、`node_exec_started/finished`、`error_occurred` 等信号解耦 UI 与执行逻辑

### 2. 执行引擎（core/executor.py）
- ✅ 从开始节点出发，沿端口连接依次推进执行
- ✅ 子线程（QThread + moveToThread）运行，不阻塞 UI
- ✅ 执行控制：暂停 / 恢复 / 终止
- ✅ 节点状态实时反馈：运行中 → 成功（含耗时）/ 失败，直接显示在节点上
- ✅ 上游输出自动收集，按端口名传递给下游节点
- ✅ 全局上下文（context.py）支持跨节点共享数据（如窗口句柄）

### 3. 节点系统
- ✅ 流程控制：开始、结束、条件判断（safe_eval 表达式，true/false 分支）、循环判断（次数控制，cycle/end 分支）、循环承接、等待
- ✅ 窗口操作：查找窗口、激活窗口
- ✅ 输入模拟：鼠标点击（坐标）、键盘组合键
- ✅ 图像操作：查找图片（模板匹配 / 颜色定位双模式，输出坐标/中心坐标/匹配度）

### 4. 自动化引擎（automation/）
- ✅ 窗口管理：查找窗口、激活窗口、等待窗口出现（uiautomation）
- ✅ 鼠标控制：移动、点击、双击、右键、拖拽、滚轮
- ✅ 键盘控制：按键、组合键、特殊按键
- ✅ 元素定位：按 AutomationId / Name / ClassName 等属性查找控件
- ✅ 图像识别：屏幕模板匹配（OpenCV + numpy）、多点颜色定位、区域/窗口内检索、像素采样

### 5. 用户界面
- ✅ 菜单栏：文件（新建/打开/保存/另存为/退出）、编辑（撤销/重做/清空）、运行（执行/验证）、工具
- ✅ 工具栏：新建、打开、保存、清空、执行、暂停、恢复、终止、验证
- ✅ 左侧节点库面板 + 中央画布 + 底部日志面板
- ✅ 设置对话框、关于对话框
- ✅ 全局 QSS 主题样式（暗色主题）
- ✅ 节点选中后属性栏编辑参数（坐标、标题、次数、表达式等）
- ✅ 查找图片节点自定义属性编辑器（`ui/node_image_widget.py`）：图片粘贴/拖拽/浏览/截屏、缩略图预览、模板匹配与颜色定位模式切换、阈值/偏移/容差/预处理/自动点击、颜色点采集、后台线程"测试识别"

### 6. 工具与校验
- ✅ 工作流验证：开始/结束节点必须唯一且各一个，开始无输入连接、结束无输出连接
- ✅ safe_eval：基于 AST 白名单的安全表达式求值
- ✅ 日志系统：控制台 + 面板彩色日志输出

### 7. 快捷键支持
- ✅ Ctrl+N / Ctrl+O / Ctrl+S / Ctrl+Shift+S / Ctrl+Q
- ✅ Ctrl+Z / Ctrl+Y
- ✅ F5 执行 / F6 验证

## 工作流执行流程

1. 用户点击"执行"（或 F5）→ 主窗口先调用工作流验证
2. 验证通过 → `CoreManager` → `WorkflowExecutor.start()` 创建子线程与 Worker
3. Worker 从开始节点出发：收集上游输出 → `node.execute(inputs)` → 根据返回的 `next_port` 推进到下游节点
4. 每个节点的开始/结束事件通过事件总线发射，主线程槽函数更新节点状态显示与日志
5. 遇到结束节点、`next_port` 为空、终止请求或异常时停止，发射执行完成信号

## 技术栈

- **GUI框架**: PyQt5 5.15.11
- **节点图库**: NodeGraphQt 0.6.44
- **UI自动化**: uiautomation 2.0.29、pywin32 311
- **图像识别**: opencv-python 4.13、numpy 2.4
- **其他**: pyperclip（剪贴板）
- **Python版本**: 3.11+
- **开发环境**: Windows 11

## 运行说明

```bash
pip install -r requirements.txt
python -m app.main
```

## 待完善功能

- [ ] 更多节点类型（如文本输入、窗口等待、截图等）
- [ ] OCR 识别节点（依赖 Tesseract / Windows OCR，暂缓）
- [ ] 查找图片多模板支持（依次匹配返回最佳）
- [ ] 节点拖拽创建（当前从节点库添加）
- [ ] 工作流调试功能（断点、单步）
- [ ] 执行历史与报告导出
- [ ] 图标资源完善

## 结论

项目已实现"可视化编排 → 验证 → 子线程执行 → 实时状态反馈"的完整闭环，具备窗口自动化所需的流程控制、窗口操作、输入模拟、图像识别能力，节点通过自动发现机制注册，新增自动化能力只需在 `nodes/` 下添加文件即可扩展。
